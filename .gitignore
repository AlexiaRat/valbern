# deps
node_modules/

# build output
dist/
*.zip

# terraform — keep .terraform.lock.hcl versioned for reproducible provider pins
infra/.terraform/
infra/build/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!example.tfvars

# env / secrets — NEVER commit
.env
.env.*
*.pem

# editor / os
.DS_Store
.idea/
.vscode/
*.log
